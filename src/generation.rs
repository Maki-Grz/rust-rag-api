use crate::types::Passage;

pub fn generate_answer(question: &str, context: &[Passage]) -> String {
    // TODO: appeler le modèle de génération avec le contexte
    String::from("This is a placeholder answer.")
}
